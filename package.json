{
  "name": "the-candid-heart",
  "version": "1.0.0",
  "description": "The Candid Heart - Frontend and Backend",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview",
    "install-deps": "cd frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 